#!/bin/bash
#make-run.sh
#make sure utserver is running


#export DISPLAY=:0 #needed if you are running a simple gui app.

#process=utserver
#makerun="utserver -daemon"

#if ps ax | grep -v grep | grep $process > /dev/null
#  then
#    exit
#  else
#    $makerun 
#    echo "utserver restart Date: " $(date) >> /home/flipsorry/cron/utserver.log
#fi

exit
